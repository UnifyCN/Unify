description: "AI Companion regression evals — newcomer-to-Canada RAG chat"

providers:
  - id: rag-query
    config:
      provider: ./lib/callRag.ts

defaultTest:
  options:
    provider:
      id: openrouter:anthropic/claude-haiku-4.5
      config:
        apiBaseUrl: https://openrouter.ai/api/v1
        apiKeyEnvar: OPENROUTER_API_KEY

tests:
  - file://cases/*.yaml
